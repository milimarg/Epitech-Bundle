import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    secondary: {
      main: '#fcfcfc',
      dark: '#595959',
      contrastText: '#030303',
    },
  },
});

export default theme;
